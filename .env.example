# YouTube credentials
YT_EMAIL=your-google-email@gmail.com
# Leave YT_PASSWORD empty to log in manually (for 2FA etc.)
YT_PASSWORD=your-google-password

# Default tags (used if not set per-file in schedule.csv)
VIDEO_TAGS=tag1,tag2,tag3
